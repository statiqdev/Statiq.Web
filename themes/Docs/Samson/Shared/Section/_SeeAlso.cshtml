@{
	IReadOnlyList<string> seeAlso = Model.List<string>("SeeAlso");
}
@if(seeAlso != null && seeAlso.Count > 0)
{
	<h1>See Also</h1>
	<ul class="list-unstyled">
		@foreach(string seeAlsoComment in seeAlso)
		{
			<li>@Html.Raw(seeAlsoComment)</li>
		}
	</ul>
}